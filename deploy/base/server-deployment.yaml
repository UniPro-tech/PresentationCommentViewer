apiVersion: apps/v1
kind: Deployment

metadata:
  name: server

spec:
  replicas: 1

  selector:
    matchLabels:
      app: server

  template:
    metadata:
      labels:
        app: server

    spec:
      containers:
        - name: server

          image: ghcr.io/YOUR_USER/presen-comeview-server:v1

          ports:
            - containerPort: 3001

          env:
            - name: PORT
              value: "3001"

            - name: HOST
              value: "0.0.0.0"

            - name: BASE_URL
              value: "comeview.example.com"

          resources:
            requests:
              cpu: 100m
              memory: 128Mi

            limits:
              cpu: 500m
              memory: 512Mi
